#include "mai.h"
/**
 * _islower - printing lower case lettrs
 * @c: the check tobe checked
 * Return: 1 if lower otherwise 0
 */
int _islower(int c);
{
if(c >= 97 && c <= 122)
{
return (1);
}
return (0);
}
